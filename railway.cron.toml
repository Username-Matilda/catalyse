[build]
builder = "NIXPACKS"
# trigger.mjs has no dependencies on the app. The default `npm run build` runs
# `prisma generate`, which needs DATABASE_URL, and this service has none.
buildCommand = "true"

[deploy]
startCommand = "node scripts/cron-trigger/trigger.mjs"
restartPolicyType = "NEVER"
